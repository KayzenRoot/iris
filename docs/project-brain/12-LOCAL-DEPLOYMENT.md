# IRIS Local Deployment

Current phase deploys governance/integration scaffolding only.

Required operator environment: Git; Python 3.12+; local HIVE v1.0.0 checkout/runtime; Docker Desktop/Compose for HIVE; optional local GEF v1.0.0 source checkout.

Machine-specific paths use environment variables. Product runtime deployment is deferred.
