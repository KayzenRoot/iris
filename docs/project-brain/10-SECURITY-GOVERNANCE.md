# IRIS Security Governance

Never commit keys/secrets/private paths. Treat prompts, models/assets, external files, DCC scenes and future plugins/MCP inputs as untrusted until validated. HIVE MCP is read-only in stable v1.0.0 integration. Git retains truth. Future DCC/model execution requires sandboxing/path controls/threat analysis. HIGH/CRITICAL security defects block promotion.
